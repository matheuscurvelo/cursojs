const ps = document.querySelector('.paragrafos').querySelectorAll('p')
const bgColorB

for (const i in ps) {
    console.log(ps[i].innerHTML);
}

for (const iterator in ps) {
    console.log(iterator.innerHTML);
}